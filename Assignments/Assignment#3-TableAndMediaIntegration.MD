**Assignment #3**  
**Date:** Friday, August 30, 2024  
**Topic:** Advanced HTML Elements  
**Submission Date:** Monday, September 2, 2024

**Task:**

1. **Create a table with 8 columns of your choice** and populate it with 15 entries. Ensure the table includes borders.

2. **Insert an image** using the `<img>` tag. Set the image’s width to 285 pixels and height to 190 pixels. Place the image in a folder named `images`.

3. **Insert an audio file** in MP3 format. Place the audio file in a folder named `audios`.

4. **Insert a video file** in MP4 format. Set the video’s width to 250 pixels and height to 150 pixels. Place the video file in a folder named `videos`.

**Folder Structure:**

- `images/` - for image files
- `audios/` - for audio files
- `videos/` - for video files

Ensure all files are correctly linked and paths are properly set.